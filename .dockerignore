node_modules
dist
coverage
*.db
*.db-shm
*.db-wal
.git
# Never copy real environment files into the image. Exclude every .env variant
# while still allowing the committed .env.example template through.
.env
.env.*
!.env.example
*.tsbuildinfo
