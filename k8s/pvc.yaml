apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: h3-prompt-studio-data
  namespace: jcode
  labels:
    app.kubernetes.io/name: h3-prompt-studio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
