apiVersion: v1
kind: Secret
metadata:
  name: h3-prompt-studio-secrets
  namespace: jcode
  labels:
    app.kubernetes.io/name: h3-prompt-studio
type: Opaque
# IMPORTANT: this is an empty placeholder. Real MiniMax credentials must be
# supplied out-of-band (e.g. `kubectl create secret`, Sealed Secrets, or a
# managed secret store) and must NEVER be committed with a real value.
# Only required when PROVIDER_MODE=minimax.
stringData:
  MINIMAX_API_KEY: ""
